Place optional Element Classic branding assets here (controller machine).

Examples:
- logo.svg
- background.jpg

On playbook run, if matrix_web_assets_sync_from_controller=true,
contents of this directory are copied to matrix_web_assets_host_path
(default: /opt/matrix/cinny/assets on the managed host).
